version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "${PORT:-5001}:${PORT:-5001}"
    volumes:
      - .:/app
    env_file:
      - .env
    command: ["sh", "-c", "flask run --host=0.0.0.0 --port=${PORT:-5001}"]
